## 2. DataLoader

`dataset\lm_dataset.py` 文件中实现了 Pretrain，SFT，DPO，RLHF 四种数据处理，我们先实现 Pretrain 和 SFT 的数据处理，剩下的强化学习方法等用到再补充。

```python
class PredtrainedDataset(Dataset):
    """
    用于语言模型的无监督预训练（类似 GPT 的自回归预训练），核心任务是 “预测下一个 token”。
    """
	pass

class SFTDataset(Dataset):
    """
    用于语言模型的有监督指令微调（Supervised Fine-Tuning, SFT），核心任务是 “根据指令和对话历史，生成符合要求的回复”（如聊天机器人、问答系统）。
    """
    pass
```

## 